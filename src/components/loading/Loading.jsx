import { Main } from "./Loading.styles";
import { CircularProgress } from "@mui/material";

function Loading() {
  return (
    <Main>
      <CircularProgress />
    </Main>
  );
}

export default Loading;